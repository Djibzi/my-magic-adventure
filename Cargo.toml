[package]
name = "my-magic-adventure"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.15"
rand = "0.8"
noise = "0.9"

# Optimisations en mode debug pour ne pas ramer
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
